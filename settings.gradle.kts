pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

includeBuild("convention-plugins")
rootProject.name = "uncaught-exception-handling"
